# ──────────────────────────────────────────────
# Copy this file to .env and fill in the values
# ──────────────────────────────────────────────

# Database
DB_USER=ticketflow
DB_PASSWORD=change_me
DB_NAME=ticketflow
DB_PORT=5432

# Backend
BACKEND_PORT=8080

# Frontend
FRONTEND_PORT=4200

# JSON Web Token — CHANGE THIS in production (min 32 random chars)
JWT_SECRET=ticketflow-secret-key-ticketflow-secret-key-123456
JWT_EXPIRATION=1h